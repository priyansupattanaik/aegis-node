[build]
  base = "apps/web"
  command = "npm run build"
  # Let Netlify Next.js runtime automatically set the publish folder (.next)

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
